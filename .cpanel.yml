---
deployment:
  tasks:
    - export DEPLOYPATH=/home/rhrrist/public_html
    - /bin/cp -R $DEPLOYPATH{,.bak}/
    - /bin/cp -R /home/rhrrist/repositories/discovery-skillhub-cccu
    - /bin/rm -rf $DEPLOYPATH.bak/
